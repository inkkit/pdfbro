[package]
name = "server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Folio HTTP server (Gotenberg-compatible)"

[[bin]]
name = "folio-server"
path = "src/main.rs"

[dependencies]
